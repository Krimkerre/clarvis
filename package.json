{
  "name": "clarvis",
  "displayName": "Clarvis",
  "description": "Clippy's presence. Jarvis's competence. A butler's disdain.",
  "version": "0.0.1",
  "publisher": "Krimkerre",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Krimkerre/clarvis.git"
  },
  "engines": {
    "vscode": "^1.93.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "clarvis",
          "title": "Clarvis",
          "icon": "media/bowtie.svg"
        }
      ]
    },
    "views": {
      "clarvis": [
        {
          "id": "clarvis.butler",
          "name": "Clarvis",
          "type": "webview"
        }
      ]
    },
    "commands": [
      {
        "command": "clarvis.debug.setState",
        "title": "Clarvis: Set Avatar State (Debug)"
      },
      {
        "command": "clarvis.setFishKey",
        "title": "Clarvis: Set Fish Audio API Key"
      },
      {
        "command": "clarvis.clearFishKey",
        "title": "Clarvis: Clear Fish Audio API Key"
      },
      {
        "command": "clarvis.testVoice",
        "title": "Clarvis: Test Voice"
      }
    ],
    "configuration": {
      "title": "Clarvis",
      "properties": {
        "clarvis.watch.minDurationSeconds": {
          "type": "number",
          "default": 30,
          "description": "Tasks/commands/debug sessions shorter than this are tracked but never notified about."
        },
        "clarvis.voice.enabled": {
          "type": "boolean",
          "default": false,
          "description": "Speak briefings and task-completion notices aloud. Off until you ask for it \u2014 a voice that surprises you once is a voice you disable forever."
        },
        "clarvis.voice.selectedVoice": {
          "type": "string",
          "default": "curated:default",
          "description": "Voice to speak with. 'system' for the OS voice, or 'fish:<reference_id>' for a Fish Audio voice."
        },
        "clarvis.voice.fishAudio.engine": {
          "type": "string",
          "default": "s2.1-pro-free",
          "enum": [
            "s2.1-pro-free",
            "s2.1-pro",
            "s2-pro",
            "s1",
            "s1-mini",
            "speech-1.6"
          ],
          "description": "Fish Audio TTS engine. Quality vs speed vs cost; the default is their current best model on a free developer tier."
        },
        "clarvis.voice.dailyRequestCap": {
          "type": "number",
          "default": 200,
          "description": "Fish Audio requests per day before dropping to the system voice. A butler should not run up a bill while you're at lunch."
        }
      }
    }
  },
  "scripts": {
    "build": "node esbuild.js",
    "watch": "node esbuild.js --watch",
    "package": "vsce package",
    "check-types": "tsc --noEmit",
    "test": "rm -rf out && tsc --outDir out && node --test out/**/*.test.js"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.93.0",
    "@vscode/vsce": "^3.0.0",
    "esbuild": "^0.23.0",
    "typescript": "^5.5.0"
  }
}
